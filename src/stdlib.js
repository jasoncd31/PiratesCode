// STANDARD LIBRARY

import { Type } from "./core.js"

export const types = Object.freeze({
    int: Type.INT,
    doubloon: Type.DOUBLE,
    booty: Type.BOOLEAN,
    shanty: Type.STRING,
    none: Type.NONE,
})
